package overseer

import (
	"fmt"
)

// Collect any artefacts created and remove traces of an executed job.
func (o *Overseer) harvest(id string) (any, error) {
	return nil, fmt.Errorf("TBD: not implemented")
}
